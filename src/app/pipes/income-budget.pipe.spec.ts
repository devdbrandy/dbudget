import { IncomeBudgetPipe } from './income-budget.pipe';

describe('IncomeBudgetPipe', () => {
  it('create an instance', () => {
    const pipe = new IncomeBudgetPipe();
    expect(pipe).toBeTruthy();
  });
});
